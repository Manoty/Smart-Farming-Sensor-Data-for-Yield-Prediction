name: smart_farming
version: "1.0.0"
config-version: 2

profile: smart_farming

model-paths: ["models"]
seed-paths: ["seeds"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  smart_farming:
    staging:
      +materialized: view
    marts:
      +materialized: table
    
